export type MessageProps = {
  name: string;
  email: string;
  message: string;
  slug: string;
  read: boolean;
};

export type PopupProps = {
  name: string;
  email: string;
  message: string;
};
